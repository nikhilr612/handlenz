#ifndef UTILS_H
#define UTILS_H

#include <stdlib.h>
#include <string.h>
#include <stdio.h>

// Read the contents of the file into a heap allocated string.
char* readFile(const char* filename);

#endif